# Session Persistence Diagnostics Requirements

# Core dependencies
anthropic>=0.40.0  # Anthropic API client (required for sdk_integration.py)
claude-agent-sdk>=0.1.46  # Optional: For session history APIs (Tests 1-2)

# Optional: For enhanced JSONL parsing
# jsonlines>=3.1.0
